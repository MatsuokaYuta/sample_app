<!--サクセスメッセージを表示-->
<% if flash[:notice]%>
    <%= flash[:notice] %>
<% end %>

<p><%= @user.name %></p>
<p><%= @user.email %></p>

<% if @user.id == @current_user.id %>
    <%= link_to("編集", "/users/#{@user.id}/edit") =%>
    
    <%= link_to("削除", "/users/#{@user.id}/destroy", {method: "post"}) =%>
<% end %>

<%= link_to("トップへ", "/") =%>