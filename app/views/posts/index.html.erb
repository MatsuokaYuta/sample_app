<h1>投稿一覧</h1>

<!--サクセスメッセージを表示-->
<% if flash[:notice]%>
    <%= flash[:notice] %>
<% end %>

<% @posts.each do |post| %>
    <p><%= link_to("●#{post.user.name}", "/users/#{post.user.id}") %></p>
    <p><%= link_to(post.content, "/posts/#{post.id}") %></p>
<% end %>
